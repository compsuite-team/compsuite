import os

SCRIPT_DIR = os.path.dirname(os.path.realpath(__file__))  # Dir of this script
REPO_DIR = SCRIPT_DIR + '/repos'
LOG_DIR = SCRIPT_DIR + '/logs'
INCOMPATIBILITY_JSON_FILE = SCRIPT_DIR + '/incompatibilities.json'
